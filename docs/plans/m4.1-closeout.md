# Plan — M4.1 close-out (complete)

**Status:** complete 2026-09-02. Kept so the bridge table has no dangling row and so the M4.1 DoD is
readable in one place.

## Goal

Satisfy every clause of `docs/ROADMAP.md` M4.1 on `main`: separate cooker, POD-C `asset_parsers`,
`.mba` v1 header + typed payload, single runtime `asset_load`, brute-force byte-deterministic re-cook,
and a CMake `content` target that gates the game binaries.

## What landed where

| DoD clause | Landing |
|---|---|
| `cooker.exe` links shared POD-C `asset_parsers` | PR #57 → `6571ee4` (`tools/cooker/`, `engine/asset_parsers/`) |
| `MbaHeader` magic+version+type+id, typed payload, hard reject on mismatch | PR #57 (`engine/asset_parsers/include/assets/mba.h`, ADR-0015, `tests/assets/mba_tests.cpp`) |
| One runtime `asset_load` switching on type tag | PR #57 (`engine/assets/include/assets/assets.h`) |
| Byte-deterministic re-cook | PR #57 `cooker_cli` test; PR #63 `sandbox_baked_content` triple-compare |
| CMake `content` target gates the game | PR #63 → `4f66af1` (`CMakeLists.txt`, `tools/sandbox/CMakeLists.txt`) |
| Runtime loads the baked texture via the single loader | PR #63 (`tools/sandbox/src/main.cpp`) |

## Residual paper (done in the PM baseline lane)

- `docs/slate-moba-phase4-m4.1.md` — evidence record.
- `docs/JOURNAL.md` Sessions 14 and 15.
- `docs/ROADMAP.md` M4.0/M4.1 status lines and the Phase 4 / Phase 5 banners.

## Not part of M4.1

Typed non-texture payloads (`content-typed-payloads`), PNG/inflate (M4.2), glTF (M4.3), hot reload
(M4.4), incremental cooking, catalogs.
